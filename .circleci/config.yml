version: 2.1
orbs:
  newman: postman/newman@0.0.2
jobs:
  newman-collection-run:
    executor: newman/postman-newman-docker
    steps:
      - checkout
      - newman/newman-run:
          collection: ./DatumandSchema.postman_collection.json
           environment: http://dev-alb-833442216.us-east-1.elb.amazonaws.com
workflows:
  version: 2.1
  deploy:
    jobs:
      - newman-collection-run:
          context: BarrelSandbox
